#include <stdio.h>

int main() {
    char a[10];
    char b[10];
    char c[10];
    printf("한글a:   "); scanf("%s", a);
    printf("한글b:   "); scanf("%s", b);
    printf("한글c:   "); scanf("%s", c);

    return 0;
}
